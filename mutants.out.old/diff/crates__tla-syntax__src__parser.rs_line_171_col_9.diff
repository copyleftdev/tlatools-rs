--- crates/tla-syntax/src/parser.rs
+++ replace Parser::unit -> Result<Unit> with Ok(Default::default())
@@ -163,20 +163,17 @@
         Ok(Module {
             name,
             extends,
             units,
         })
     }
 
     fn unit(&mut self) -> Result<Unit> {
-        self.fences.push(1);
-        let parsed = self.unit_body();
-        self.fences.pop();
-        parsed
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn unit_body(&mut self) -> Result<Unit> {
         let local = self.eat(&Tok::Kw(Kw::Local));
         match self.peek().clone() {
             Tok::Kw(Kw::Constant) => {
                 self.advance();
                 Ok(Unit::Constants(self.decl_list()?))
