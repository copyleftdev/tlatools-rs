--- crates/tla-syntax/src/parser.rs
+++ delete ! in Parser::module
@@ -132,17 +132,17 @@
         let out = f(self);
         self.fences.pop();
         out
     }
 
     // ---------------------------------------------------------------- module
 
     fn module(&mut self) -> Result<Module> {
-        while !(matches!(self.peek(), Tok::Separator)
+        while  /* ~ changed by cargo-mutants ~ */(matches!(self.peek(), Tok::Separator)
             && matches!(self.peek_at(1), Tok::Kw(Kw::Module)))
         {
             if matches!(self.peek(), Tok::Eof) {
                 return Err(self.err("no `---- MODULE ----` header found"));
             }
             self.advance();
         }
         self.advance();
