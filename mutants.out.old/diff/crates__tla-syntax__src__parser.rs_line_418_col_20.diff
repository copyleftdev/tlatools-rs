--- crates/tla-syntax/src/parser.rs
+++ delete ! in Parser::opt_params
@@ -410,17 +410,17 @@
         Ok(Decl { name, arity })
     }
 
     fn opt_params(&mut self) -> Result<Vec<Param>> {
         let mut params = Vec::new();
         if self.eat(&Tok::LParen) {
             loop {
                 params.push(self.param()?);
-                if !self.eat(&Tok::Comma) {
+                if  /* ~ changed by cargo-mutants ~ */self.eat(&Tok::Comma) {
                     break;
                 }
             }
             self.expect(&Tok::RParen)?;
         }
         Ok(params)
     }
 
