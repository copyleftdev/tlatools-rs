--- crates/tla-syntax/src/lexer.rs
+++ replace Lexer::scan_equals -> Result<Tok> with Ok(Default::default())
@@ -356,42 +356,17 @@
                     });
                 }
                 Some(c) => s.push(c),
             }
         }
     }
 
     fn scan_equals(&mut self) -> Result<Tok> {
-        let run = self.run_of('=');
-        if run >= 4 {
-            self.advance(run);
-            return Ok(Tok::ModuleEnd);
-        }
-        if run == 2 {
-            self.advance(2);
-            return Ok(Tok::DefEq);
-        }
-        if run == 1 {
-            return Ok(match self.at(1) {
-                Some('>') => {
-                    self.advance(2);
-                    Tok::Op(Op::Implies)
-                }
-                Some('<') => {
-                    self.advance(2);
-                    Tok::Op(Op::Le)
-                }
-                _ => {
-                    self.bump();
-                    Tok::Op(Op::Eq)
-                }
-            });
-        }
-        Err(self.err("`===` is neither a definition nor a module terminator"))
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     /// A run of four or more dashes is a separator, so the operators spelled
     /// with dashes have to be recognised around it rather than before it.
     fn scan_dashes(&mut self) -> Tok {
         if self.starts_with("-+->") {
             self.advance(4);
             return Tok::Op(Op::User("-+->"));
