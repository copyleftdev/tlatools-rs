--- crates/tla-eval/src/spec.rs
+++ replace == with != in Spec::find_instance
@@ -163,17 +163,17 @@
         seen: &mut BTreeSet<usize>,
     ) -> Option<&Instance> {
         if !seen.insert(module) {
             return None;
         }
         let here = self.facts[module]
             .instances
             .iter()
-            .find(|i| i.name.as_deref() == Some(name));
+            .find(|i| i.name.as_deref() != /* ~ changed by cargo-mutants ~ */ Some(name));
         if here.is_some() {
             return here;
         }
         self.facts[module]
             .extends
             .iter()
             .find_map(|&parent| self.find_instance(parent, name, seen))
     }
