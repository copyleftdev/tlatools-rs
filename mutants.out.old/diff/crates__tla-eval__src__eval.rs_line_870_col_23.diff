--- crates/tla-eval/src/eval.rs
+++ replace /= with %= in Evaluator<'m>::fn_set
@@ -862,17 +862,17 @@
             .unwrap_or(usize::MAX);
         check_size(count, "a function set")?;
 
         let mut out = BTreeSet::new();
         for mut index in 0..count {
             let mut entries = BTreeMap::new();
             for key in &keys {
                 entries.insert(key.clone(), values[index % values.len()].clone());
-                index /= values.len();
+                index %= /* ~ changed by cargo-mutants ~ */ values.len();
             }
             out.insert(Value::function(entries));
         }
         if keys.is_empty() {
             out.insert(Value::Seq(Vec::new()));
         }
         Ok(Value::Set(out))
     }
