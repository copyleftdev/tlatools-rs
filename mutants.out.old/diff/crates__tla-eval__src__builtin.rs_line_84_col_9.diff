--- crates/tla-eval/src/builtin.rs
+++ delete match arm "ToString" in call
@@ -76,20 +76,17 @@
         "Assert" => {
             arity(2)?;
             if args[0] == Value::Bool(true) {
                 Ok(Value::Bool(true))
             } else {
                 Err(Error::Malformed(format!("assertion failed: {}", args[1])))
             }
         }
-        "ToString" => {
-            arity(1)?;
-            Ok(Value::Str(args[0].to_string()))
-        }
+         /* ~ changed by cargo-mutants ~ */
         "Print" => {
             arity(2)?;
             Ok(args[1].clone())
         }
         "PrintT" => {
             arity(1)?;
             Ok(Value::Bool(true))
         }
