--- crates/tla-syntax/src/parser.rs
+++ delete ! in Parser::skip_expression_label
@@ -571,17 +571,17 @@
                         }
                     }
                     Tok::Eof => return None,
                     _ => {}
                 }
                 ahead += 1;
             }
         }
-        if !matches!(self.peek_at(ahead), Tok::ColonColon) {
+        if  /* ~ changed by cargo-mutants ~ */matches!(self.peek_at(ahead), Tok::ColonColon) {
             return None;
         }
         for _ in 0..=ahead {
             self.advance();
         }
         Some(self.expr(0))
     }
 
