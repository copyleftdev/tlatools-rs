--- crates/tla-syntax/src/parser.rs
+++ replace >= with < in Parser::expr
@@ -478,17 +478,17 @@
             }
         }
         Ok((module, subs))
     }
 
     // ------------------------------------------------------------ expression
 
     fn expr(&mut self, min_prec: u8) -> Result<Expr> {
-        if self.depth >= MAX_NESTING {
+        if self.depth < /* ~ changed by cargo-mutants ~ */ MAX_NESTING {
             return Err(self.err(format!("expressions nest more than {MAX_NESTING} deep")));
         }
         self.depth += 1;
         let parsed = self.expr_inner(min_prec);
         self.depth -= 1;
         parsed
     }
 
