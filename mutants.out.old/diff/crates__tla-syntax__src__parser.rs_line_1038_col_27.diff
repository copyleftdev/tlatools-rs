--- crates/tla-syntax/src/parser.rs
+++ replace -= with += in Parser::subscript_follows_bracket
@@ -1030,17 +1030,17 @@
                         Tok::Ident(name) => name.starts_with('_'),
                         _ => false,
                     };
                 }
                 Tok::RParen | Tok::RBrack | Tok::RBrace | Tok::RTup => {
                     if depth == 0 {
                         return false;
                     }
-                    depth -= 1;
+                    depth += /* ~ changed by cargo-mutants ~ */ 1;
                 }
                 Tok::Eof => return false,
                 _ => {}
             }
         }
         false
     }
 
