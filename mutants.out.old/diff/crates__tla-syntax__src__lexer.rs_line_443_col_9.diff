--- crates/tla-syntax/src/lexer.rs
+++ replace Lexer::scan_slash -> Result<Tok> with Ok(Default::default())
@@ -435,26 +435,17 @@
                 return tok;
             }
         }
         self.bump();
         Tok::Op(Op::Gt)
     }
 
     fn scan_slash(&mut self) -> Result<Tok> {
-        if self.starts_with("/\\") {
-            self.advance(2);
-            return Ok(Tok::Op(Op::And));
-        }
-        if self.starts_with("/=") {
-            self.advance(2);
-            return Ok(Tok::Op(Op::Neq));
-        }
-        self.user_symbol()
-            .map_or_else(|| Err(self.err("stray `/`")), Ok)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn scan_backslash(&mut self) -> Result<Tok> {
         if self.starts_with("\\/") {
             self.advance(2);
             return Ok(Tok::Op(Op::Or));
         }
         let start = self.pos + 1;
