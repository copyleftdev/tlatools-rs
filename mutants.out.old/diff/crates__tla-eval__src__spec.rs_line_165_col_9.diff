--- crates/tla-eval/src/spec.rs
+++ replace Spec::find_instance -> Option<&Instance> with None
@@ -157,30 +157,17 @@
     }
 
     fn find_instance(
         &self,
         module: usize,
         name: &str,
         seen: &mut BTreeSet<usize>,
     ) -> Option<&Instance> {
-        if !seen.insert(module) {
-            return None;
-        }
-        let here = self.facts[module]
-            .instances
-            .iter()
-            .find(|i| i.name.as_deref() == Some(name));
-        if here.is_some() {
-            return here;
-        }
-        self.facts[module]
-            .extends
-            .iter()
-            .find_map(|&parent| self.find_instance(parent, name, seen))
+        None /* ~ changed by cargo-mutants ~ */
     }
 }
 
 struct Builder<'a, M: Modules> {
     modules: Vec<Module>,
     index: BTreeMap<String, usize>,
     source: &'a M,
 }
