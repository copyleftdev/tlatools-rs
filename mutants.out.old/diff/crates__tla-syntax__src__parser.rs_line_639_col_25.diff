--- crates/tla-syntax/src/parser.rs
+++ delete match arm Tok::Colon in Parser::postfix
@@ -631,20 +631,17 @@
                         Tok::Num(n) => {
                             self.advance();
                             n.to_string()
                         }
                         Tok::Op(op) => {
                             self.advance();
                             op.symbol().to_string()
                         }
-                        Tok::Colon => {
-                            self.advance();
-                            String::new()
-                        }
+                         /* ~ changed by cargo-mutants ~ */
                         _ => self.expect_ident()?,
                     };
                     let mut args = Vec::new();
                     if self.eat(&Tok::LParen) {
                         args = self.bracketed(|p| p.expr_list(&Tok::RParen))?;
                         self.expect(&Tok::RParen)?;
                     }
                     e = Expr::Qualified {
