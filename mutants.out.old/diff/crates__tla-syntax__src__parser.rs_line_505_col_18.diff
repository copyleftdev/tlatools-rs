--- crates/tla-syntax/src/parser.rs
+++ replace == with != in Parser::junction
@@ -497,17 +497,17 @@
         let col = self.col();
         self.fences.push(col);
         let mut items = Vec::new();
         while *self.peek() == Tok::Op(op) && self.col() == col {
             self.advance();
             items.push(self.expr(0)?);
         }
         self.fences.pop();
-        Ok(if op == Op::And {
+        Ok(if op != /* ~ changed by cargo-mutants ~ */ Op::And {
             Expr::conjunction(items)
         } else {
             Expr::disjunction(items)
         })
     }
 
     fn prefix(&mut self) -> Result<Expr> {
         if let Some(labelled) = self.skip_expression_label() {
