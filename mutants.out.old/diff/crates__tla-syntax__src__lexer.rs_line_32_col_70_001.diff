--- crates/tla-syntax/src/lexer.rs
+++ replace + with * in module_start
@@ -24,17 +24,17 @@
     for (index, text) in src.split_inclusive('\n').enumerate() {
         let trimmed = text.trim_start();
         if trimmed.starts_with("----")
             && trimmed
                 .trim_start_matches('-')
                 .trim_start()
                 .starts_with("MODULE")
         {
-            return (offset, u32::try_from(index).unwrap_or(u32::MAX) + 1);
+            return (offset, u32::try_from(index).unwrap_or(u32::MAX) * /* ~ changed by cargo-mutants ~ */ 1);
         }
         offset += text.len();
     }
     (0, 1)
 }
 
 impl Lexer {
     fn new(src: &str) -> Self {
