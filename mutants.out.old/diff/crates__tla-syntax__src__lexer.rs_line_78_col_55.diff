--- crates/tla-syntax/src/lexer.rs
+++ replace == with != in Lexer::starts_with
@@ -70,17 +70,17 @@
 
     fn advance(&mut self, n: usize) {
         for _ in 0..n {
             self.bump();
         }
     }
 
     fn starts_with(&self, s: &str) -> bool {
-        s.chars().enumerate().all(|(i, c)| self.at(i) == Some(c))
+        s.chars().enumerate().all(|(i, c)| self.at(i) != /* ~ changed by cargo-mutants ~ */ Some(c))
     }
 
     fn run_of(&self, c: char) -> usize {
         let mut n = 0;
         while self.at(n) == Some(c) {
             n += 1;
         }
         n
