--- crates/tla-eval/src/eval.rs
+++ replace || with && in Evaluator<'m>::binary
@@ -680,17 +680,17 @@
         match op {
             Op::And => {
                 return Ok(Value::Bool(
                     self.eval_bool(lhs, ctx)? && self.eval_bool(rhs, ctx)?,
                 ));
             }
             Op::Or => {
                 return Ok(Value::Bool(
-                    self.eval_bool(lhs, ctx)? || self.eval_bool(rhs, ctx)?,
+                    self.eval_bool(lhs, ctx)? && /* ~ changed by cargo-mutants ~ */ self.eval_bool(rhs, ctx)?,
                 ));
             }
             Op::Implies => {
                 return Ok(Value::Bool(
                     !self.eval_bool(lhs, ctx)? || self.eval_bool(rhs, ctx)?,
                 ));
             }
             _ => {}
