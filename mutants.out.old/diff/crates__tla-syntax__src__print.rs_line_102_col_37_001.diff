--- crates/tla-syntax/src/print.rs
+++ replace match guard op.is_postfix() with false in bare
@@ -94,17 +94,17 @@
         } => {
             write!(f, "{instance}!{name}")?;
             if args.is_empty() {
                 Ok(())
             } else {
                 write!(f, "({})", list(args))
             }
         }
-        Expr::Unary(op, operand) if op.is_postfix() => {
+        Expr::Unary(op, operand) if false /* ~ changed by cargo-mutants ~ */ => {
             write(operand, ATOM, f)?;
             f.write_str(op.symbol())
         }
         Expr::Unary(op, operand) => {
             f.write_str(op.symbol())?;
             if op.is_word() {
                 f.write_str(" ")?;
             }
