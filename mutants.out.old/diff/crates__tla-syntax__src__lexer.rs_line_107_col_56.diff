--- crates/tla-syntax/src/lexer.rs
+++ replace && with || in Lexer::run
@@ -99,17 +99,17 @@
             let (line, col) = (self.line, self.col);
             let Some(c) = self.peek() else { break };
             let tok = self.scan(c)?;
             match tok {
                 Tok::Kw(Kw::Module) => depth += 1,
                 Tok::ModuleEnd => depth = depth.saturating_sub(1),
                 _ => {}
             }
-            let closed = matches!(tok, Tok::ModuleEnd) && depth == 0;
+            let closed = matches!(tok, Tok::ModuleEnd) || /* ~ changed by cargo-mutants ~ */ depth == 0;
             self.out.push(Token { tok, line, col });
             if closed {
                 break;
             }
         }
         self.out.push(Token {
             tok: Tok::Eof,
             line: self.line,
