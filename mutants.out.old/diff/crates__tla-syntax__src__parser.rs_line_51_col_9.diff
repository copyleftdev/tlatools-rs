--- crates/tla-syntax/src/parser.rs
+++ replace Parser::peek -> &Tok with Box::leak(Box::new(Default::default()))
@@ -43,17 +43,17 @@
         Self {
             toks,
             pos: 0,
             fences: Vec::new(),
         }
     }
 
     fn peek(&self) -> &Tok {
-        &self.toks[self.pos].tok
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     fn peek_at(&self, offset: usize) -> &Tok {
         let i = (self.pos + offset).min(self.toks.len() - 1);
         &self.toks[i].tok
     }
 
     fn col(&self) -> u32 {
