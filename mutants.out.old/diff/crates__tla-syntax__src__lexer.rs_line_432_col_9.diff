--- crates/tla-syntax/src/lexer.rs
+++ replace Lexer::scan_gt -> Tok with Default::default()
@@ -424,24 +424,17 @@
                 return tok;
             }
         }
         self.bump();
         Tok::Op(Op::Lt)
     }
 
     fn scan_gt(&mut self) -> Tok {
-        for (text, tok) in [(">>", Tok::RTup), (">=", Tok::Op(Op::Ge))] {
-            if self.starts_with(text) {
-                self.advance(text.len());
-                return tok;
-            }
-        }
-        self.bump();
-        Tok::Op(Op::Gt)
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     fn scan_slash(&mut self) -> Result<Tok> {
         if self.starts_with("/\\") {
             self.advance(2);
             return Ok(Tok::Op(Op::And));
         }
         if self.starts_with("/=") {
