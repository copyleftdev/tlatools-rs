--- crates/tla-syntax/src/parser.rs
+++ replace == with != in Parser::eat
@@ -81,17 +81,17 @@
         let t = self.toks[self.pos].tok.clone();
         if self.pos + 1 < self.toks.len() {
             self.pos += 1;
         }
         t
     }
 
     fn eat(&mut self, tok: &Tok) -> bool {
-        if self.peek() == tok {
+        if self.peek() != /* ~ changed by cargo-mutants ~ */ tok {
             self.advance();
             true
         } else {
             false
         }
     }
 
     fn expect(&mut self, tok: &Tok) -> Result<()> {
