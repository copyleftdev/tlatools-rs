--- crates/tla-syntax/src/lexer.rs
+++ replace += with -= in Lexer::bump
@@ -58,17 +58,17 @@
 
     fn bump(&mut self) -> Option<char> {
         let c = self.chars.get(self.pos).copied()?;
         self.pos += 1;
         if c == '\n' {
             self.line += 1;
             self.col = 1;
         } else {
-            self.col += 1;
+            self.col -= /* ~ changed by cargo-mutants ~ */ 1;
         }
         Some(c)
     }
 
     fn advance(&mut self, n: usize) {
         for _ in 0..n {
             self.bump();
         }
