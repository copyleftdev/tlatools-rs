--- crates/tla-eval/src/eval.rs
+++ replace Evaluator<'m>::invoke -> Result<Value> with Ok(Default::default())
@@ -401,33 +401,17 @@
     /// an operator parameter is bound rather than evaluated.
     fn invoke(
         &self,
         def: &'m Def,
         scope: usize,
         args: &'m [Expr],
         ctx: &mut Ctx<'m, '_>,
     ) -> Result<Value> {
-        if def.params.len() != args.len() {
-            return Err(Error::Malformed(format!(
-                "`{}` takes {} argument(s), given {}",
-                def.name,
-                def.params.len(),
-                args.len()
-            )));
-        }
-        let mut bindings = Vec::with_capacity(args.len());
-        for (param, arg) in def.params.iter().zip(args) {
-            bindings.push(if param.arity == 0 {
-                Local::Val(self.eval(arg, ctx)?)
-            } else {
-                self.operator_argument(arg, ctx)?
-            });
-        }
-        self.run(&def.name, &def.body, scope, &def.params, bindings, ctx)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn enter_closure(
         &self,
         name: &str,
         params: &'m [Param],
         body: &'m Expr,
         scope: usize,
