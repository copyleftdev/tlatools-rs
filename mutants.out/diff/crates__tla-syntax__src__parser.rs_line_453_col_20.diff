--- crates/tla-syntax/src/parser.rs
+++ delete ! in Parser::param
@@ -445,17 +445,17 @@
 
     /// A formal parameter: a name, or `f(_, _)` for one that is itself an
     /// operator and so has to be applied rather than used as a value.
     fn param(&mut self) -> Result<Param> {
         let name = self.expect_ident()?;
         let mut arity = 0;
         if self.eat(&Tok::LParen) {
             loop {
-                if !self.eat(&Tok::Underscore) {
+                if  /* ~ changed by cargo-mutants ~ */self.eat(&Tok::Underscore) {
                     self.expect_ident()?;
                 }
                 arity += 1;
                 if !self.eat(&Tok::Comma) {
                     break;
                 }
             }
             self.expect(&Tok::RParen)?;
