--- crates/tla-syntax/src/lexer.rs
+++ replace += with -= in Lexer::skip_block_comment
@@ -138,17 +138,17 @@
     fn skip_block_comment(&mut self) -> Result<()> {
         let (line, col) = (self.line, self.col);
         let mut depth = 0usize;
         loop {
             if self.peek().is_none() {
                 return Err(Error::lex("unterminated (* comment", line, col));
             }
             if self.starts_with("(*") {
-                depth += 1;
+                depth -= /* ~ changed by cargo-mutants ~ */ 1;
                 self.advance(2);
             } else if self.starts_with("*)") {
                 depth -= 1;
                 self.advance(2);
                 if depth == 0 {
                     return Ok(());
                 }
             } else {
