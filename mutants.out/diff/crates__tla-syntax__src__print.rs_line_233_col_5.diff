--- crates/tla-syntax/src/print.rs
+++ replace list -> String with String::new()
@@ -225,21 +225,17 @@
     params
         .iter()
         .map(ToString::to_string)
         .collect::<Vec<_>>()
         .join(", ")
 }
 
 fn list(items: &[Expr]) -> String {
-    items
-        .iter()
-        .map(ToString::to_string)
-        .collect::<Vec<_>>()
-        .join(", ")
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 fn bounds_list(bounds: &[Bound]) -> String {
     bounds
         .iter()
         .map(ToString::to_string)
         .collect::<Vec<_>>()
         .join(", ")
