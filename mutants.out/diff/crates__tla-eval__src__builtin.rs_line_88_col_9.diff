--- crates/tla-eval/src/builtin.rs
+++ delete match arm "Print" in call
@@ -80,20 +80,17 @@
             } else {
                 Err(Error::Malformed(format!("assertion failed: {}", args[1])))
             }
         }
         "ToString" => {
             arity(1)?;
             Ok(Value::Str(args[0].to_string()))
         }
-        "Print" => {
-            arity(2)?;
-            Ok(args[1].clone())
-        }
+         /* ~ changed by cargo-mutants ~ */
         "PrintT" => {
             arity(1)?;
             Ok(Value::Bool(true))
         }
         _ => Err(Error::Undefined(name.to_string())),
     }
 }
 
