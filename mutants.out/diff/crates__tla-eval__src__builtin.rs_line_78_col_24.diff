--- crates/tla-eval/src/builtin.rs
+++ replace == with != in call
@@ -70,17 +70,17 @@
         "Seq" => {
             arity(1)?;
             Ok(Value::Infinite(Infinite::Sequences(Box::new(
                 args[0].clone(),
             ))))
         }
         "Assert" => {
             arity(2)?;
-            if args[0] == Value::Bool(true) {
+            if args[0] != /* ~ changed by cargo-mutants ~ */ Value::Bool(true) {
                 Ok(Value::Bool(true))
             } else {
                 Err(Error::Malformed(format!("assertion failed: {}", args[1])))
             }
         }
         "ToString" => {
             arity(1)?;
             Ok(Value::Str(args[0].to_string()))
