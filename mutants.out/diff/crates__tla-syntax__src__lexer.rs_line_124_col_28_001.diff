--- crates/tla-syntax/src/lexer.rs
+++ replace match guard c.is_whitespace() with false in Lexer::skip_trivia
@@ -116,17 +116,17 @@
             col: self.col,
         });
         Ok(self.out)
     }
 
     fn skip_trivia(&mut self) -> Result<()> {
         loop {
             match self.peek() {
-                Some(c) if c.is_whitespace() => {
+                Some(c) if false /* ~ changed by cargo-mutants ~ */ => {
                     self.bump();
                 }
                 Some('\\') if self.at(1) == Some('*') => {
                     while !matches!(self.peek(), None | Some('\n')) {
                         self.bump();
                     }
                 }
                 Some('(') if self.at(1) == Some('*') => self.skip_block_comment()?,
