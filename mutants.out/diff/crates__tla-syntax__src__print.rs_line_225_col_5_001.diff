--- crates/tla-syntax/src/print.rs
+++ replace params_list -> String with "xyzzy".into()
@@ -217,21 +217,17 @@
             let holes = vec!["_"; self.arity].join(", ");
             write!(f, "({holes})")?;
         }
         Ok(())
     }
 }
 
 fn params_list(params: &[Param]) -> String {
-    params
-        .iter()
-        .map(ToString::to_string)
-        .collect::<Vec<_>>()
-        .join(", ")
+    "xyzzy".into() /* ~ changed by cargo-mutants ~ */
 }
 
 fn list(items: &[Expr]) -> String {
     items
         .iter()
         .map(ToString::to_string)
         .collect::<Vec<_>>()
         .join(", ")
