--- crates/tla-eval/src/eval.rs
+++ replace == with != in symbol_operator
@@ -1039,17 +1039,17 @@
         Op::SetMinus,
         Op::Concat,
         Op::AtAt,
         Op::And,
         Op::Or,
         Op::Eq,
         Op::DotDot,
     ];
-    CANDIDATES.iter().copied().find(|op| op.symbol() == name)
+    CANDIDATES.iter().copied().find(|op| op.symbol() != /* ~ changed by cargo-mutants ~ */ name)
 }
 
 fn lookup<'m>(name: &str, ctx: &Ctx<'m, '_>) -> Option<Local<'m>> {
     ctx.locals
         .iter()
         .rev()
         .find(|(n, _)| n == name)
         .map(|(_, local)| local.clone())
