--- crates/tla-syntax/src/token.rs
+++ delete match arm "VARIABLE" | "VARIABLES" in Kw::lookup
@@ -32,17 +32,17 @@
 }
 
 impl Kw {
     pub fn lookup(word: &str) -> Option<Self> {
         Some(match word {
             "MODULE" => Self::Module,
             "EXTENDS" => Self::Extends,
             "CONSTANT" | "CONSTANTS" => Self::Constant,
-            "VARIABLE" | "VARIABLES" => Self::Variable,
+             /* ~ changed by cargo-mutants ~ */
             "LET" => Self::Let,
             "IN" => Self::In,
             "IF" => Self::If,
             "THEN" => Self::Then,
             "ELSE" => Self::Else,
             "CHOOSE" => Self::Choose,
             "CASE" => Self::Case,
             "OTHER" => Self::Other,
