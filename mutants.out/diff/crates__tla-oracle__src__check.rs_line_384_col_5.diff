--- crates/tla-oracle/src/check.rs
+++ replace render -> String with String::new()
@@ -376,14 +376,10 @@
     };
     format!(
         "{head}. The closest was `{}`, {why}: `{}` does not hold ({} of its {} conjuncts do)",
         near.action, near.conjunct, near.satisfied, near.total
     )
 }
 
 fn render(state: &State) -> String {
-    let body: Vec<String> = state
-        .iter()
-        .map(|(name, value)| format!("{name} = {value}"))
-        .collect();
-    format!("[{}]", body.join(", "))
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
