--- crates/tla-syntax/src/parser.rs
+++ delete match arm Tok::Kw(Kw::Extends) in Parser::module_body
@@ -162,23 +162,17 @@
             while self.eat(&Tok::Separator) {}
             match self.peek() {
                 Tok::ModuleEnd | Tok::Eof => break,
                 Tok::Kw(Kw::Module) => {
                     let inner = self.module_body()?;
                     self.expect(&Tok::ModuleEnd)?;
                     units.push(Unit::Inner(Box::new(inner)));
                 }
-                Tok::Kw(Kw::Extends) => {
-                    self.advance();
-                    extends.push(self.expect_ident()?);
-                    while self.eat(&Tok::Comma) {
-                        extends.push(self.expect_ident()?);
-                    }
-                }
+                 /* ~ changed by cargo-mutants ~ */
                 _ => units.push(self.unit()?),
             }
         }
         Ok(Module {
             name,
             extends,
             units,
         })
