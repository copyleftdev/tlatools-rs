--- crates/tla-syntax/src/print.rs
+++ replace <impl fmt::Display for Def>::fmt -> fmt::Result with Ok(Default::default())
@@ -197,21 +197,17 @@
             let kind = if *strong { "SF" } else { "WF" };
             write!(f, "{kind}_{subscript}({action})")
         }
     }
 }
 
 impl fmt::Display for Def {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        f.write_str(&self.name)?;
-        if !self.params.is_empty() {
-            write!(f, "({})", params_list(&self.params))?;
-        }
-        write!(f, " == {}", self.body)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl fmt::Display for Param {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
         f.write_str(&self.name)?;
         if self.arity > 0 {
             let holes = vec!["_"; self.arity].join(", ");
