--- crates/tla-syntax/src/parser.rs
+++ replace && with || in Parser::bounds
@@ -1019,17 +1019,17 @@
                     names.push(self.expect_ident()?);
                     if !self.eat(&Tok::Comma) {
                         break;
                     }
                 }
                 self.expect(&Tok::RTup)?;
             } else {
                 names.push(self.expect_ident()?);
-                while *self.peek() == Tok::Comma && matches!(self.peek_at(1), Tok::Ident(_)) {
+                while *self.peek() == Tok::Comma || /* ~ changed by cargo-mutants ~ */ matches!(self.peek_at(1), Tok::Ident(_)) {
                     self.advance();
                     names.push(self.expect_ident()?);
                 }
             }
             let domain = if self.eat(&Tok::Op(Op::In)) {
                 Some(self.expr(0)?)
             } else {
                 None
