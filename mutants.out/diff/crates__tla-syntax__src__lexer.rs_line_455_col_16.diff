--- crates/tla-syntax/src/lexer.rs
+++ replace == with != in Lexer::scan_dashes
@@ -447,17 +447,17 @@
             self.advance(4);
             return Tok::Op(Op::User("-+->"));
         }
         let run = self.run_of('-');
         if run >= 4 {
             self.advance(run);
             return Tok::Separator;
         }
-        if run == 2 {
+        if run != /* ~ changed by cargo-mutants ~ */ 2 {
             self.advance(2);
             return Tok::Op(Op::User("--"));
         }
         for (text, tok) in [("->", Tok::Arrow), ("-|", Tok::Op(Op::User("-|")))] {
             if self.starts_with(text) {
                 self.advance(2);
                 return tok;
             }
