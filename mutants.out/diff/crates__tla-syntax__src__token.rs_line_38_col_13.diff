--- crates/tla-syntax/src/token.rs
+++ delete match arm "EXTENDS" in Kw::lookup
@@ -30,17 +30,17 @@
     Enabled,
     Unchanged,
 }
 
 impl Kw {
     pub fn lookup(word: &str) -> Option<Self> {
         Some(match word {
             "MODULE" => Self::Module,
-            "EXTENDS" => Self::Extends,
+             /* ~ changed by cargo-mutants ~ */
             "CONSTANT" | "CONSTANTS" => Self::Constant,
             "VARIABLE" | "VARIABLES" => Self::Variable,
             "LET" => Self::Let,
             "IN" => Self::In,
             "IF" => Self::If,
             "THEN" => Self::Then,
             "ELSE" => Self::Else,
             "CHOOSE" => Self::Choose,
