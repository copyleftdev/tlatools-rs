--- crates/tla-eval/src/spec.rs
+++ delete ! in Spec::search
@@ -133,17 +133,17 @@
     }
 
     fn search(
         &self,
         module: usize,
         name: &str,
         seen: &mut BTreeSet<usize>,
     ) -> Option<(usize, &Def)> {
-        if !seen.insert(module) {
+        if  /* ~ changed by cargo-mutants ~ */seen.insert(module) {
             return None;
         }
         if let Some(def) = self.modules[module].definition(name) {
             return Some((module, def));
         }
         self.facts[module]
             .extends
             .iter()
